import { mkdirSync, writeFileSync } from 'node:fs';
import { recipes, mealPlanDays, getRecipe } from '../src/data/recipes.js';
const escape = value => String(value).replace(/[&<>"']/g, character => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[character]));
mkdirSync('public', { recursive: true });
writeFileSync('public/recipes.html', `<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="description" content="All twelve NutriPlate recipes, available without JavaScript."><title>NutriPlate — Text-only recipe collection</title><style>
*{box-sizing:border-box}body{max-width:850px;margin:0 auto;padding:28px 22px;font:17px/1.8 system-ui,sans-serif;color:#233a30;background:#faf8f3}a{color:#214f3f;text-underline-offset:4px}a:focus-visible{outline:3px solid #b94228;outline-offset:4px}h1,h2,h3{line-height:1.3}h1{font-size:2.3rem}article{border-top:1px solid #cad1c5;padding-top:24px;margin-top:36px}li{margin-bottom:8px}nav ul{columns:2;padding-left:22px}.note{background:#edf1e6;padding:18px}article:target{outline:2px solid #214f3f;outline-offset:12px}.skip{position:absolute;left:-9999px}.skip:focus{position:static}@media(max-width:600px){nav ul{columns:1}}@media print{body{background:white;color:black;font-size:11pt}header nav,.back{display:none}article{break-before:page}li{break-inside:avoid}}
</style></head><body><a class="skip" href="#main">Skip to recipes</a><header><a href="./">NutriPlate home</a><h1>Good food. Less guesswork.</h1><p>The complete recipe collection. Read, jump between recipes, or print this page without JavaScript.</p><nav aria-label="Recipe collection"><ul>${recipes.map(recipe => `<li><a href="#${recipe.id}">${escape(recipe.title)}</a> · ${recipe.time} min</li>`).join('')}</ul></nav></header><main id="main"><section aria-labelledby="menu-heading"><h2 id="menu-heading">Three days of inspiration</h2><p>These example menus are flexible starting points, not personalized nutrition plans.</p><ul>${mealPlanDays.slice(0, 3).map(day => `<li><strong>${day.day}:</strong> ${['breakfast', 'lunch', 'dinner'].map(meal => `<a href="#${day[meal]}">${escape(getRecipe(day[meal]).title)}</a>`).join(' · ')}</li>`).join('')}</ul></section>${recipes.map(recipe => `<article id="${recipe.id}" aria-labelledby="${recipe.id}-heading"><h2 id="${recipe.id}-heading">${escape(recipe.title)}</h2><p>${escape(recipe.description)}</p><p><strong>${recipe.time} minutes · ${recipe.servings} servings · ${recipe.meal}</strong></p><h3>Ingredients</h3><ul>${recipe.ingredients.map(ingredient => `<li>${escape(ingredient)}</li>`).join('')}</ul><h3>Method</h3><ol>${recipe.steps.map(step => `<li>${escape(step)}</li>`).join('')}</ol><p class="note"><strong>Kitchen note:</strong> ${escape(recipe.tip)}</p><p>${recipe.allergens.length ? `Includes or may include: ${escape(recipe.allergens.join(', '))}.` : ''} Check packaged ingredient labels for your needs.</p>${recipe.safety ? `<p>${escape(recipe.safety)} <a href="https://www.foodsafety.gov/food-safety-charts/safe-minimum-internal-temperatures">FoodSafety.gov guidance</a></p>` : ''}<a class="back" href="#main">Back to collection</a></article>`).join('')}</main><footer><p>Cooking times are estimates. <a href="./#/contact">Contact and feedback</a> is a local demonstration form and does not send or save messages.</p><p><a href="./">Open the interactive recipe finder</a></p></footer></body></html>`);
